import 'package:flutter/material.dart';

class CullingScreen extends StatelessWidget {
  const CullingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Culling");
  }
}